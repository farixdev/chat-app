- [x] Fix Firestore collection-name mismatches in `lib/services/firestore_service.dart` (friendRequests/friendsRequest, friendships/friendship, notifications/notification, messages/message)
- [ ] Ensure affected queries/updates compile and preserve existing public API
- [x] Run `flutter analyze` to catch syntax/type errors
- [ ] Run `flutter test` (if available) / quick manual sanity checks


